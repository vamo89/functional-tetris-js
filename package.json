{
  "name": "fuctional-tetris",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "ava",
    "lint": "standard",
    "dev": "chokidar '**/*.js' -i bundle.js -i node_modules -c 'standard && ava && browserify index.js > bundle.js'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "pre-commit": [
    "test",
    "lint"
  ],
  "devDependencies": {
    "ava": "^0.19.1",
    "browserify": "^14.3.0",
    "chokidar-cli": "^1.2.0",
    "precommit-hook": "^3.0.0",
    "standard": "^10.0.2"
  }
}
